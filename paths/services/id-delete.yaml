summary: Delete service
tags:
  - Services
security:
  - bearerAuth: [ ]
parameters:
  - in: path
    name: id
    schema:
      type: integer
    required: true
    description: Valid service id
responses:
  204:
    description: Service deleted
